const config = {
    id: String,
    username: String,
    password: String,
    created: Date,
    roles: Array,
    pfp: String,
    last_logged_in: Date,
    ip: String,
    banned: Boolean,
    discord_id: String
}

module.exports = config